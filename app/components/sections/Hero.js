import Logo from '../ui/Logo';

export default function Hero() {
  return (
    <section className="w-screen h-screen bg-oboro-pink flex justify-center items-center">
      <Logo />
    </section>
  );
}

